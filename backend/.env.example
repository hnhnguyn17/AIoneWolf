# ─── AIoneWolf backend ───
PORT=3636

# Auth ví Solana
JWT_SECRET=doi-thanh-secret-that
REQUIRE_AUTH=0           # =1 để bắt buộc đăng nhập ví mới được vào socket

# Shared secret giữa backend <-> service ai/ (AI Quản trò)
GM_SECRET=dev-gm-secret

# Solana
SOLANA_CLUSTER=devnet
SOLANA_RPC=https://api.devnet.solana.com
# SOLANA_KEYPAIR=          # secret key ví backend (mint NFT) dưới dạng JSON array [1,2,3...] hoặc chuỗi Base58 — KHÔNG commit

# Agora (để trống = stub; điền khi nối thật)
AGORA_APP_ID=
AGORA_APP_CERT=
REFRESH_SECRET=doi-refresh-secret-that
